import 'package:flutter/material.dart';

class PlaylistModel extends StatelessWidget {
  const PlaylistModel({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
